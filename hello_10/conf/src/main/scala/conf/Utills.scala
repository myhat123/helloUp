package conf

object Utils {
    def sum(a: Int, b: Int): Int = {
        a + b
    }
}

object CassSetting {
    val host = "localhost"
    val username = "cassandra"
    val password = "cassandra"
}